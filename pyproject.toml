[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "rpg-translator-suite"
dynamic = ["version"]
description = "A modular localization toolkit for RPG Maker and similar game engines."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MPL-2.0" }
authors = [{ name = "Noobzin14" }]
dependencies = [
    "PySide6>=6.7,<7",
    "httpx>=0.25,<1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8,<9",
]

[project.scripts]
rts = "main:main"

[tool.setuptools.dynamic]
version = { attr = "app.core.version.__version__" }

[tool.setuptools]
packages = ["app", "app.core", "app.gui", "plugins", "plugins.rpgmaker_mv"]
py-modules = ["main"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
